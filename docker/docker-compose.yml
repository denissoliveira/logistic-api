# Use postgres/example user/password credentials
version: '3.1'

services:

  db:
    image: postgres:alpine
    restart: "no"
    environment:
      POSTGRES_PASSWORD: admin
    ports:
      - 5432:5432  

# Administrador em PHP
  #adminer:
   #image: adminer
   #restart: always
   #ports: - 8080:8080